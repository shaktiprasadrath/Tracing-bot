# W1 — F07/F08 Decision-Register Reconciliation Report

Scope: `docs/architecture/features/F07-correlation.md`, `docs/architecture/features/F08-memory.md`
against `docs/architecture/06-decision-register.md` (DR-0…DR-39, Appendix C).

Method: grepped every "Docs to change" line in the register, filtered for `F07`/`F08` mentions,
read each matching DR in full, then read both target docs in full and diffed content against each
DR's mandated text (interfaces, config keys, numbers, FR/AC IDs).

## DRs applicable to F07 (per "Docs to change" lines)

DR-0 (fact-ownership/§3.2 NFR + §4.3 config), DR-2 (`F07 §4.4`), DR-4 (model types, cited generally),
DR-5 (tenant-first-param), DR-16 (`F07 §4.3` — literal `Contains`), DR-20 (`F07 §3.1` FR-F07-6/-7/-8,
§3.2, §4.3, §4.4, §6, §7), DR-31 (every `F0x §4.3` constructor signature), DR-38 (`F07 §3.1` FR-F07-8).

## F07 — status: DONE

All DRs above were already correctly applied by the prior (interrupted) run — verified by direct text
comparison, not by trusting the `Revision 2` header:
- FR-F07-6/-7/-8 and AC-F07-6/-7/-8 present, matching Appendix C exactly.
- `LogAdapter`/`MetricAdapter`/`Correlator` interfaces copied verbatim with `tid model.TenantID` as
  first param (DR-5, DR-20 §20.1).
- Bounded fan-out (semaphore 4/tenant, 16 global), breaker (5 failures → 30s), tenant-partitioned LRU
  cache (2048 entries) all verbatim per DR-20 §20.4.
- `file` dev adapter (NDJSON logs, `.prom` metric snapshots) verbatim per DR-20 §20.5.
- `Contains` literal-match-after-retrieval (DR-16 §16.2) stated in FR-F07-5, §4.4 algorithm, §6.
- `model.Window`/`model.LogLine`/etc. cited, not re-declared (DR-0, DR-4).
- Mermaid flowchart (§4.1) and sequence diagram (§4.5) both valid.

**Gap found and fixed this run:** DR-31 (`model.Clock` injection) was **not applied** — no constructor
signature existed in §4.3, no clock-determinism NFR row, and DR-31 was missing from the header line
despite being mandated ("every `F0x §4.3` constructor signature"). Fixed:
- Added `func New(cfg Config, clock model.Clock, logAdapters []LogAdapter, metricAdapters []MetricAdapter, dialer auth.EgressDialer) (Correlator, error)` to §4.3, matching the pattern used in F01/F02/F03 (comment explains cache-TTL and breaker timing read `model.Clock`, never `time.Now`/`time.After`).
- Added a "Determinism" row to the §3.2 NFR table citing DR-31.
- Updated header to `Revision 3` and added `DR-31` to the applied-DR list.

Remaining: none identified.

## DRs applicable to F08 (per "Docs to change" lines)

DR-0, DR-2 (`F08 §4.3`), DR-4 (`F08 §4.2`), DR-5, DR-19 (`F08 §3.1` FR-F08-4/-7/-9/-10, §3.2, §4.2,
§4.3, §4.4, §6, §7, §8), DR-29 (`F08 §4.3`), DR-31 (constructor signature), DR-32 (`F08 §3.2`,
"unchanged, now uncontradicted"), DR-34 (`F08 §4.3` — embedder holds `llm.Client`), DR-38 (`F08 §4.2`
— `Kind`).

## F08 — status: DONE

All DRs above were already correctly applied by the prior run — verified by direct comparison:
- FR-F08-9/-10 new, FR-F08-4/-7 rewritten, AC-F08-3 rewritten, AC-F08-7 extended — matches Appendix C
  exactly.
- `memory.Store.Record(ctx, tid, rec model.InvestigationRecord)` — NOT `rca.Investigation` — verbatim,
  with the DR-2 cycle-break comment ("memory imports … llm … never rca").
- `Fingerprint{TenantID, Tokens, Hash}` with `TenantID` always first, hash preimage
  `tenantID‖"\x00"‖join(Tokens,"\x01")` verbatim (DR-19 §19.1).
- Bounded `Similar()` (rarest-first posting-list union, ≤500 candidates, DR-19 §19.2) verbatim.
- Correction arithmetic (+0.15/‑0.35, caps/floors, decay-at-read-only, `SupersedesID`) verbatim
  (DR-19 §19.4).
- Export determinism rules (sort order, RFC3339 UTC, LF, `effectiveWeight` excluded) verbatim
  (DR-19 §19.6/FR-F08-10).
- `SimilarityScorer`/`Embedder`/`Store`/`RunbookImporter` interfaces copied verbatim with
  `tid model.TenantID` second param everywhere (DR-5, DR-19 §19.3).
- Postgres+pgvector box deletion / "no external vector DB" claim now uncontradicted (DR-32 §32.1).
- `anthropic` scorer shares `internal/llm.Client` with `rca`, no separate `AnthropicEmbedder` (DR-34
  §34.3).
- `Kind` field (`Investigation`/`Runbook`/`Correction`/`ServiceMeta`) present per DR-38 §38.1.
- Mermaid flowchart (§4.1) and sequence diagram (§4.5) both valid.

**Gap found and fixed this run:** same DR-31 gap as F07 — no clock-injected constructor, no
determinism NFR row, DR-31 absent from header. Fixed:
- Added `func New(cfg Config, clock model.Clock, scorer SimilarityScorer, embedder Embedder, db store.DB) (Store, error)` to §4.3 (comment: correction decay `age(LastUsedAt)` and the nightly consolidation
  schedule read `model.Clock`, never `time.Now`/`time.After`).
- Added a "Determinism (clock)" row to the §3.2 NFR table citing DR-31.
- Updated header to `Revision 3` and added `DR-31` to the applied-DR list.

Remaining: none identified.

## Ambiguities / notes for review

- DR-31's mandate ("every `F0x §4.3` constructor signature") is not accompanied by a specific function
  name or full parameter list for F07/F08 in the register text itself (unlike DR-19/DR-20 which give
  exact signatures) — the added `New(...)` signatures were inferred from the pattern already
  established in F01/F02/F03 (`func New(cfg Config, clock model.Clock, ...)`) and from each package's
  own dependency list stated elsewhere in the same doc (F07 §4.1 diagram deps: log/metric adapters,
  `auth.EgressDialer`; F08 §4.1/§4.3: `SimilarityScorer`, `Embedder`, SQLite `store.DB`). If the
  register's owning doc (`02 §1`–`§4`) declares a different exact parameter list, that should win —
  flagging for cross-check against `02` rather than re-deciding here (out of scope for this task).
- No other content gaps were found; the prior interrupted run's `Revision 2` header was accurate for
  everything except the DR-31 omission.

## Line counts (after edits)

- F07-correlation.md: 400 lines (was 393)
- F08-memory.md: 418 lines (was 410)
