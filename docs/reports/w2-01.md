# w2-01 — Apply DR-10..DR-39 to 01-system-architecture.md

STATUS: **PARTIAL** — remaining DRs: DR-14 through DR-39 (26 of 30 DRs in this run's scope
not yet applied).

Continuation of `w1-01.md`, which applied DR-0..DR-9. This pass applied DR-10 through DR-13
in ascending order, then stopped at the ~15-minute time box given the size of the remaining
DRs (several — DR-14, DR-23, DR-25, DR-36 — are individually as large as what was done this
pass).

File grew from 2210 to 2429 lines. Header line now reads:
`> Revision 2 — 2026-09-15 — applies DR-0, DR-1, ..., DR-9, DR-10, DR-11, DR-12, DR-13 (round-1 fixes)`

## DRs applied

- **DR-10** (sampler canonical interface, keep-rate cap, baselines) — `01 §4.2` rewritten in
  full: the `Sampler` interface, `Stats`/`ShardStats`, `PolicyEvaluator`, `BaselineSource`
  (never called from the decision path), `KeyBaseline`/`BaselineSnapshot`, and `Decision`
  (now `Reason model.KeepReason`, not a local `sampler.Reason` type — the old `type Reason
  uint8` const block is **deleted**). Added the six-keep-class ordering, the `max_keep_rate`
  hard-cap shed order, and the `PathSignature` ordered-edge-list definition (replaces the old
  service-set description). `01 §7` `sampler.policy.*` gained `rare_path_keeps_per_min`,
  `max_path_signature_cardinality`, closed-enum comment on `slow_quantile`; new
  `sampler.baseline.refresh_interval` block added.
- **DR-11** (interest predicate, two phases) — folded into the same `01 §4.2` rewrite:
  `PredicateScope`/`ScopeInvestigation`/`ScopeRecurrence`, the full `InterestPredicate`
  struct (services/operations/attr/errorSig/pathSig/traceID bounds), phase A/B push-and-remove
  rules, eviction rule, bounded-matching description, `Hits`/`SecondaryReasons` accuracy rule,
  and the 4-level narrowing ladder. `01 §7` `sampler.interest.*` block replaced in full
  (`max_predicates`, new `max_recurrence_predicates`, `max_trace_ids_per_predicate`,
  `scope_ttl`/`recurrence_ttl` replacing the old single `default_ttl`, `narrow_at_keep_rate`,
  `persist`).
- **DR-12** (cost control loop) — new subsection appended to `01 §5.3` with the `store.CostSignal`
  /`Watermark` types, the proportional-controller description, the full escalation ladder table
  (Warn/High/Critical), and the `RetentionPolicy` struct (`MaxDiskBytes`/`HighWatermark`/
  `ActionOnFull`/`ByteBudgetBytes`); notes `store.Trace.KeptReason sampler.Reason` is deleted
  in favor of `model.KeepReason`. `01 §7` gained a new `store.cost.*` block (`signal_interval`,
  `measure_window`, `deadband`, `max_step`, `recovery_ramp`, `adaptive_floor_*`,
  `settling_target`) and `store.budget.byte_budget_per_tenant_gb` with its startup-validation
  comment. `01 §9` D-D1 row rewritten to cite `FR-F03-15`/`AC-F03-15` and `FR-F06-17`.
- **DR-13** (topology: one input, bounded graph, full `Graph` interface) — `01 §4.7` rewritten
  in full: pre-sampling ingest-fan-out input rule, `Edge` struct with `Resolution` and
  `model.LatencyHist` (drops `CalleeOperation` from the edge key and `P50/P95/P99Nanos`
  fields), the full `Graph`/`Neighborhood`/`EdgeSink`/`EdgeSource` interfaces, the
  `Changes()`-lossless-for-`NewEdge` rule, and the 80 MiB memory-budget note. `01 §2` mermaid:
  `RED --> TG` replaced with `LIM --> TG` (pre-sampling input). `01 §5.1` DDL: `topology_edge`
  gained `resolution` (new PK component) and `hist BLOB` replacing `p50/p95/p99_nanos`;
  `topology_edge_meta` dropped `callee_operation`; new `topology_edge_op` DDL added (was
  referenced in the file-inventory table at line 1076 but had no DDL — now defined per DR-13's
  spec). `01 §7` `topology.max_edges` 200000 → 20000, new `topology.max_operations_per_edge:
  20`. `01 §10.2`'s RSS table already cited "Topology graph at `max_edges: 20000` (DR-13) —
  80 MiB", so no change was needed there (it was pre-applied ahead of DR-13 during the DR-9
  pass — verified consistent, not edited).

## DRs remaining (not started), with their `01` targets

Unchanged from `w1-01.md` for DR-14 onward:

| DR | `01` sections still to change |
|---|---|
| DR-14 | §4.3 (`anomaly.Event`/`Incident`), §5.1, §7, §10.1 |
| DR-15 | §4.4 (`rca.Journal` not `StepStore`) |
| DR-16 | §6.3 (replaced by new §16.2–§16.4), §8.6(4) |
| DR-17 | §4.4 (`Budget`/`Spend`), §7, §10.3 |
| DR-18 | §4.4 (Step/Investigation fields), §5.1, §6.1 (`/replay?mode=`) |
| DR-19 | §4.6 (Provenance/TrustTier), §5.1, §7, §8.6(2) |
| DR-20 | §7, §8.7 (SSRF/tenant scoping) |
| DR-21 | §7, §9 (D-D4), §10.3 |
| DR-22 | §4.1/§4.5 (typed remediation payloads, delete `Params map[string]string`), §8.6(6) |
| DR-23 | §4.5, §5.1 (`action`/`idempotency` DDL), §6.1 (`/rollback`), §7, §8.2 |
| DR-24 | §1.2 (`internal/k8s` executor replaces `remediate.KubectlExecutor`) |
| DR-25 | §8.1–§8.5 (identity/RBAC matrix), §5.1, §6.1, §7 |
| DR-26 | §7, §8.1 (TLS floor), §8.4 (limits table), §6.1 |
| DR-27 | §5.1 (`audit_anchor`), §6.1, §7, §8.5 |
| DR-28 | §3.1, §7, §10.1, §10.2 (allocation budgets, delete zero-alloc claim) |
| DR-29 | §6.1, §6.2 (12 MCP tools + roles), §7 |
| DR-30 | §9 (each row names its F12 screen) |
| DR-31 | §4.1 (`Clock`/`Ticker`/`Timer`/`Barrier` — partially present from DR-4), §1.2 |
| DR-32 | §3.2, §7 (driver keys/defaults), §10.1 (formula set) |
| DR-33 | §7 (`ops.*`) |
| DR-34 | §7 (`rca.llm.*`), §10.3 (cost targets) |
| DR-35 | §7 (`nl.context_turns`) |
| DR-36 | §5.1 (`eval_result`), §7, §10.3 |
| DR-37 | §8.6 (normative, `UntrustedKind`) |
| DR-38 | §7 (`topology.envoy_access_logs`), §9 |
| DR-39 | §3.1, §4.1 (`redCh` row, struct), §4.7, §5.1 (`red_rollup*` columns, drop p90), §7, §10.1 |

Appendix C (new FR/AC IDs, register lines 3855–end) still not consulted.

Note: applying DR-14 will require re-touching `01 §7` and `01 §10.1`/`§10.2` again after this
pass's edits — no conflict expected since DR-10..DR-13 didn't touch `anomaly.*` keys, but the
next session should re-`sed` the current file rather than trust cached line numbers, since this
pass shifted everything after line ~616 by roughly +220 lines.

## Ambiguities / judgment calls (most-literal reading taken)

1. **DR-13's `topology_edge_op` DDL.** The table was already named in `01 §5.1`'s per-file
   inventory list (added during an earlier DR-6/DR-9 pass) but had no `CREATE TABLE` statement
   anywhere in the document. This pass added the DDL verbatim from DR-13's register text next
   to `topology_edge_meta`, since DR-13 is the owning decision and nothing else defines it.
2. **DR-13's §10.2 RSS row.** The "80 MiB @ `max_edges: 20000` (DR-13)" row in `01 §10.2` was
   already present and correct before this pass touched anything (evidently added during the
   `w1-01` DR-9 pass, which cites DR-13 by name for that row). Left as-is; verified it matches
   DR-13's own "≈ 80 MiB at the cap" figure exactly, so no edit was needed or made.
3. **`Decision.Reason` typing.** DR-10's register text doesn't restate the full `Decision`
   struct (only the `Sampler` interface, `Stats`, etc.); the struct fields are carried over
   from the prior `01 §4.2` content, retyped `Reason Reason` → `Reason model.KeepReason` per
   DR-4 (already-applied, binding) and DR-10's explicit statement that `sampler.Reason` no
   longer exists as a persisted type. `MatchedPredicateID`/`SecondaryReasons` fields added per
   DR-11's `Hits`/`SecondaryReasons` accuracy rule, which implies they live on `Decision`.

## Verification not performed

Mermaid diagram edit (`RED --> TG` → `LIM --> TG`) checked by eye only, not rendered. New/edited
tables' column counts checked visually, not linted. No cross-check against `02`, `03`, `04`, or
the `F0x`/`X-*` feature docs (out of scope per the task's scope lock). SQL DDL edits
(`topology_edge`, `topology_edge_meta`, new `topology_edge_op`) were not run through a SQLite
parser.
