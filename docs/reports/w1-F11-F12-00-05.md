# Worker report — F11, F12, 00, 05

Scope: apply every decision-register DR whose "Docs to change" names F11 or F12; spot-check 00 and
05 against the verified toolchain/pin facts. Edited only: `F11-eval-harness.md`,
`F12-ui-api-integrations.md`, and this report. `00-feature-catalog.md` and
`05-tool-selection-adr.md` were read-only spot-checked (no edits needed — see below).

## F11-eval-harness.md

**DRs verified/applied:** DR-0 (§3.2 NFR values annotated as owned by `01 §10`; config-keys
section made citation-only per `01 §7`), DR-5 (tenant scoping noted via `RunOptions.Tenant`),
DR-15 (§4.4 scorer now matches `rca.Hypothesis.Category`/`.Component` directly — no shadow
`ServiceOrComponent`/`Narrative` types), DR-21 (§7 notes AC-F12-16's alert-precision measurement
is owned jointly, F11=measurement/F12=mechanism), DR-24 (FR-F11-3 deleted; istioctl/kubectl direct
path removed), DR-31 (`model.Clock`/`eval.VirtualClock`/`Barrier` wired through §4.1/§4.4/§4.5),
DR-36 (the whole runner rewritten: `Mode`/`Isolation`/`RunOptions`/`ScenarioSpec`/`Expectation`/
`EvidenceCategory` types verbatim from DR-36 §36.1/§36.4; no-bypass via `ingest.Receiver`;
absolute-floor + regression CI gates table; `ModeLive` routed through `remediate.Guard`; new
FR-F11-11/-12/-13 and AC-F11-11..14), DR-38 (FR-F11-6's "expected evidence categories" term
resolved via the closed enum; open questions marked resolved instead of open).

**Remaining / ambiguities:**
- `Runner` interface (§4.3) is reproduced **verbatim** from DR-36 §36.1 (`Run(ctx, opts)`, no
  `tid` positional parameter), even though DR-5's general rule lists `eval` among tenant-scoped
  packages requiring `ctx, tid` second-parameter ordering. I flagged this tension inline rather
  than silently resolving it — DR-36 is the more specific, later decision and gives the interface
  its exact shape, so I kept it verbatim and noted `RunOptions.Tenant` as the tenant-scoping
  mechanism for this package. A future DR may want to reconcile this explicitly.
- `eval/suites/istio-transaction-lab/` was renamed to `eval/scenarios/istio-transaction-lab/`
  to match DR-36 §36.7's `eval.scenarios_dir` key and `RunOptions.ScenariosDir`/`List(dir)`
  signature (the old doc used `eval/suites/...` and a `LoadSuite`/`WriteReport`-based `Runner`
  that DR-36 replaces wholesale). Flagging this path rename in case it conflicts with an
  already-authored external scenario catalog.
- Did not restate `01 §5.1`'s `eval_result` DDL or `01 §7`'s `eval.*` defaults verbatim in F11 —
  per DR-0, those are cited, not restated (I list key paths only, no values, in §4.3).

## F12-ui-api-integrations.md

**DRs verified/applied:** DR-0 (§3.2 NFR and config-keys sections turned into citations of
`01 §10`/`01 §7`), DR-18 (new FR-F12-12/-13, replay endpoint `?mode=recorded|live-diff`, replay
diff view added to Investigations screen and FR-F12-4), DR-21 (FR-F12-8 rewritten to the three
paging conditions P1/P2/P3, `api.AlertRouter`, deadman FR-F12-15, no severity bypass, no
`ServiceMeta.Tier` input), DR-23 (idempotency key changed from `X-Slack-Request-Id` to the
chat-derived `sha256(platform|team_id|message_ts|action_id|verb)` key; `/actions/{id}/rollback`
endpoint added; nonexistent Slack header deleted), DR-25 (RBAC reframed as a capability matrix,
not `admin ⊇ approver ⊇ operator ⊇ viewer`; chat identity binding — HMAC is authenticity only,
`auth.IdentityStore.Resolve` is authorization; `audit:read` corrected to admin), DR-26 (canonical
config key names `api.endpoint`/`auth.tls.min_version`; TLS 1.3 floor noted in §6), DR-27 (audit
role = admin, `?verify=true`, hash-chained + externally anchored audit cited), DR-29 (endpoint
table re-prefixed `/v1` per `01 §6.1`; roles corrected — audit=admin, replay=operator,
correction=step-scoped only, investigation-level `/correct` deleted; MCP moved to `/v1/mcp` with
the twelve-tool table and per-tool `MinRole`, `traceiq_propose_action` withheld — FR-F12-6
rewritten), DR-30 (nine screens — added Eval/Cost & Retention/Memory as FR-F12-16/-17/-18;
`api.Server` struct renamed `api.HTTPServer` + `api.Deps`; untrusted-marker FR-F12-19), DR-36
(§4.4's `PagerDutyPager.OnIncidentCandidate` — which called the nonexistent
`rca.Engine.PreliminaryReport`/`.FinalReport` — deleted and replaced by `api.AlertRouter` polling
`Investigation.Status`), DR-37 (FR-F12-19 untrusted, model-authored rationale marker beside the
Guard-reconstructed payload; AC-F12-17), DR-38 (AC renumbering: added AC-F12-12..17 contiguous
with the existing set).

**Remaining / ambiguities:**
- The full `01 §6.1` endpoint table (DR-29 §29.1) adds several rows not previously in F12 at all
  (`/v1/identities`, `/v1/sampler/*`, `/v1/store/budget`, `/v1/baselines`, `/v1/deploys`,
  `/v1/tenants/{id}/erasure`) — I included the ones F12's own screens consume (identities, eval,
  sampler/store for Cost & Retention) in the filtered view but did not exhaustively reproduce every
  row from DR-29 §29.1 that belongs to other features (e.g. `/v1/baselines`, `/v1/deploys` are
  F05's, not rendered by any F12 screen) — per DR-0/DR-29, F12 should render **only** the rows its
  own screens consume, which is what I did; flagging in case the intent was a full mirror instead.
- `WebhookHandler.SlackSigningSecret`/`IdentityStore`/`IdempotencyStore` in §4.2 replaced the old
  `PagerDutyRoutingKeys map[string]string` field with no explicit DR forcing its removal (DR-23
  didn't mention it) — I kept it removed since no DR text referenced it as canonical and it wasn't
  load-bearing to the rewritten flows shown; worth re-adding if `01 §7`'s `alerting.pagerduty.*`
  block expects per-tenant routing-key config on this struct.

## 00-feature-catalog.md — spot-check

Header already reads `> Revision 2 — 2026-09-15 — applies DR-0, DR-3, DR-4, DR-5, DR-10, DR-15,
DR-25, DR-38 (round-1 fixes)`. Content already reflects the F11/F12 rewrite: `eval.Runner`
described as driving the real `ingest.Receiver` with no bypass (DR-31/DR-36); `api.Server` already
listed as "renamed `api.HTTPServer` + `api.Deps`"; MCP already described as "twelve-tool MCP
endpoint" (DR-29/DR-30); `k8s.Executor` and the `internal/k8s`/`internal/cluster` packages present
(DR-24/DR-27); the PRD-promise table matches DR-38 §38.1 verbatim, including the `ReplayReReason`
and canary-rollout Phase-3 deferrals. **No edits made — already correct and consistent with the
F11/F12 rewrite above.**

## 05-tool-selection-adr.md — spot-check

Header already reads `> Revision 2 — 2026-09-15 — applies DR-0, DR-1, DR-6, DR-9, DR-24, DR-31,
DR-32, DR-34 (round-1 fixes)`. Verified against the task's confirmed facts:
- §8.1 "Set A" is headed **WITHDRAWN 2026-09-14**, retained only as a historical record, with an
  explicit "No document may pin from Set A" note.
- §8.2 "Set B — normative pins" declares `go.mod` verbatim with `module traceiq`, `go 1.27`,
  `toolchain go1.27.1`, and every direct pin matches the given verified facts exactly: `otlp
  v1.11.0`, `grpc v1.83.2`, `protobuf v1.36.12`, `modernc.org/sqlite v1.58.0`, `parquet-go
  v0.32.0`, `x/sync v0.23.0`, `klauspost/compress v1.20.0`, `x/time v0.16.0`, `x/crypto v0.57.0`,
  `go-cmp v0.7.0`, `prometheus/client_golang v1.24.1`, `otel`+`otel/sdk v1.46.0`.
- Analyser pins are re-pinned in the same section ("Analyser pins, re-pinned in the same atomic
  change (DR-1)"), each declaring Go ≥ 1.27 support.
- §9 FU table shows FU-1 **CLOSED** (Go 1.27.1 landed, Set B normative, 0 reachable
  vulnerabilities — matching the verified `govulncheck` fact) and FU-2 **CLOSED — not needed**,
  plus new FU-7/-8/-9/-10/-11.
- `GOTOOLCHAIN=go1.27.1` is specified as the CI environment value with `GOTOOLCHAIN=auto` and
  `GOTOOLCHAIN=local` both explicitly forbidden (§8.2), and the pre-migration `GOTOOLCHAIN=local`
  references in §7/§8.1 are clearly scoped to the withdrawn Go-1.23 era, not left as live
  contradictions.

**No edits made — already correct and fully consistent with the verified facts given.**

## Summary

| Doc | Status | Lines |
|---|---|---|
| F11-eval-harness.md | DONE | 372 → 517 |
| F12-ui-api-integrations.md | DONE | 382 → 538 |
| 00-feature-catalog.md | VERIFIED, no changes needed | 124 |
| 05-tool-selection-adr.md | VERIFIED, no changes needed | 626 |

No remaining DRs outstanding for F11/F12 against the "Docs to change" grep. Two ambiguities noted
above (F11 `Runner` tenant-parameter tension with DR-5; F12's non-owned endpoint rows) are
judgment calls made in favor of the more specific/later DR and the doc's own screen scope,
respectively — not left undone, but worth a second pair of eyes.
