# Worker report — F01–F04

## F01-ingest.md — COMPLETE (verified, no changes made)
- Header (line 3) applies: DR-0, DR-2, DR-3, DR-4, DR-9, DR-26, DR-28, DR-31.
- Cross-checked against every "Docs to change" line in `06-decision-register.md` naming `F01` — set matches exactly, nothing missing or extra.
- Spot-checked content: `New(cfg Config, clock model.Clock, resolver tenant.Resolver, sinks []SpanSink)` (DR-31 Clock injection) present at line 196; `tenant.Resolver.FromSubject`/`FromDevDefault` tenant-resolution rule (DR-3/DR-5/DR-26) present at lines 54/88/119.
- 386 lines. No gaps found.

## F02-tail-sampler.md — COMPLETE (verified, no changes made)
- Header (line 3) applies: DR-0, DR-3, DR-4, DR-8, DR-9, DR-10, DR-11, DR-12, DR-31, DR-32, DR-39.
- Cross-checked against every "Docs to change" line naming `F02` — matches exactly.
- Spot-checked content: `model.REDSample` sole-RED-type note (DR-39) at lines 116–117; `Consume(ctx, tid, spans) ... satisfies ingest.SpanSink structurally (DR-2)` at line 58; bus-default decision recorded in §8 as "Decided (round 1, DR-8 §8 / DR-32 §32.3)" at line 587 (initial grep for the literal string "bus default" missed this — found on direct §8 read, not a real gap).
- 606 lines. No gaps found.

## F03-tiered-storage.md — COMPLETE (verified, no changes made)
- Header (line 3) applies: DR-0, DR-2, DR-3, DR-4, DR-5, DR-6, DR-7, DR-12, DR-13, DR-31, DR-33, DR-39.
- Cross-checked against every "Docs to change" line naming `F03` — matches exactly.
- Spot-checked content: DR-6 two-file schema/PRAGMA ownership note and hot-index ratio (line 25, 61); DR-33 durability rewrite (`synchronous=NORMAL`/`FULL`, RPO figures) at line 73; DR-4/DR-6/DR-7/DR-39 deletions (`ColdPointer`, `SpanRollup`, `REDResult`, `REDBucket`, `TopologyEdgeRow` → `topology.Edge` per DR-13) at lines 124–125.
- 609 lines. No gaps found.

## F04-topology.md — COMPLETE (fully rewritten this session)
Was NOT STARTED at session start (no header, stale 4-method `Graph` interface, direct `store.Store` coupling, `EdgeRED`/p90 still present, `/api/v1/*` paths of its own, no Clock injection, no tenant parameter on methods).

**DRs whose "Docs to change" line names `F04`** (confirmed by `grep -n "Docs to change"` over the full register, filtered by literal `F04` match): DR-0, DR-2, DR-5, DR-13, DR-29, DR-31, DR-38, DR-39. All eight applied; header line added listing them.

Changes applied, by DR:
- **DR-0** — §3.2 NFR table now cites `01 §10.2` as owning authority rather than standing alone.
- **DR-2** — `internal/topology` imports only `model`, `config`, `tenant`; never `store` or `ingest`. Persistence now flows through feature-declared `EdgeSink`/`EdgeSource` interfaces (satisfied by `store/sqlite`/`store/clickhouse`, wired by `cmd/traceiq`), replacing the old direct `store.Store` calls in §4.1/§4.4. `topology.LiveGraph` satisfies `ingest.SpanSink` structurally.
- **DR-5** — every `Graph`/`EdgeSink`/`EdgeSource` method takes `(ctx, tid model.TenantID, ...)` in that order.
- **DR-13** (largest change) — replaced `Observe(span)`/`ServiceNode`+`Edge`+`EdgeRED` model with `Consume(ctx, tid, []model.Span)` and the DR-13 `Edge`/`Resolution` struct verbatim; restored the full 10-method `Graph` interface verbatim (`Consume, Neighbors, Distance, Edges, EdgeOps, Snapshot, Export, Changes, Flush, Stats`); added `EdgeSink`/`EdgeSource` interfaces verbatim; `topology.max_edges` 200,000→**20,000** with LRU eviction; memory budget rewritten to **≈80 MiB**; `Changes()` NewEdge made lossless via `topology_edge_meta.is_new` reconciliation (documented in §4.4/§5/§7); §8 sharding question marked **Decided (round 1): hash(caller, callee)**, reflected in §4.1/§4.4/§5.
- **DR-29** — deleted F04's own `/api/v1/topology/*` endpoint table; §4.3 now a filtered view header "defined in `01 §6.1`" plus the two MCP tools (`traceiq_topology_neighbors`, `traceiq_topology_edges`, both viewer role).
- **DR-31** — added `NewLiveGraph(ctx, tid, cfg, sink, source, clock model.Clock)` constructor; Clock use (bucket boundaries, `vanished_after`) noted in §5.
- **DR-38** — added new **FR-F04-10** (Envoy access-log adapter, `topology.envoy_access_logs.enabled: false` default) and **AC-F04-10** (recorded Istio bundle test).
- **DR-39** — `topology.EdgeRED` deleted (superseded by DR-13's `Edge`), no stored p90 anywhere in the doc.

**Appendix C IDs applied exactly**: FR-F04-10 added (§3.1, now 10 FRs total); AC-F04-9 and AC-F04-10 added (§7, now 10 ACs total, one-to-one with FRs).

**§2 drawback table**: both rows now cite the DR mechanism (D-Z2 → DR-13; D-J5 → DR-2, DR-13).

**Template sections 1–8 / 4.1–4.5 preserved**; both mermaid blocks (component diagram §4.1, sequence diagram §4.5) revised in place and are syntactically valid flowchart/sequenceDiagram blocks.

**Ambiguities flagged in §8 (not resolved, since resolving them would be re-deciding, out of scope)**:
- `EdgeOp`, `Snapshot`, `Stats`, `ExportFormat` Go struct shapes are not given verbatim anywhere in the register — DR-13 lists the `Graph` interface signatures that return/take them but never spells out their fields. I inferred minimal shapes from `topology_edge_op`'s DDL columns and the pre-existing design, and flagged them explicitly in both §4.2 (inline comments) and §8 as needing lead-architect confirmation rather than presenting them as binding.

499 lines (was 393).

## Remaining
None. All four files (F01–F04) are complete against the register as scoped. No edits were made outside `F04-topology.md` and this report — the register and all other docs were left untouched.
