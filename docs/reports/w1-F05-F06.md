# Verification report — F05 / F06 (round 1 DR application)

Scope: verify/complete `docs/architecture/features/F06-rca-engine.md` (full) and spot-check
`docs/architecture/features/F05-anomaly-detection.md`, against every DR in
`docs/architecture/06-decision-register.md` whose "Docs to change" line names F06 or F05.
No edits were made to either file — both were already correct on inspection. The register and
other docs were not touched, per scope lock.

## DR → doc mapping used (from `grep -n "Docs to change"`, filtered)

- F06: DR-0 (generic §3.2/§4.3), DR-4, DR-5, DR-11, DR-14, DR-15, DR-16, DR-17, DR-18, DR-19,
  DR-22, DR-29, DR-31 (generic), DR-34, DR-37, DR-38.
- F05: DR-0 (generic), DR-4, DR-5, DR-13, DR-14, DR-17, DR-21, DR-29, DR-31 (generic), DR-35,
  DR-38, DR-39.
- Note: DR-2 and DR-3, which the task brief flagged as "expected," do **not** name F05 or F06 in
  their "Docs to change" lines (checked verbatim) — they were correctly excluded.

## F06-rca-engine.md — STATUS: COMPLETE (verified, no changes needed)

Header line lists exactly the DR set above (matches the grep-derived set precisely).

Verified point-by-point:
- **Closed 5-tool set (DR-16):** `ToolArgs` union type with exactly 5 typed pointer fields;
  `ToolArgs.Query`/`.Extra` explicitly marked deleted; `ToolRegistry.Names()` returns exactly
  five; semantic validation (`ValidateToolArgs`) and the PromQL template allowlist reproduced
  in full.
- **Token/cost budget arithmetic (DR-17):** §4.4 worked arithmetic table (S/C/M/D/K/V/O/N)
  reproduces `01 §4.4`'s numbers verbatim, explicitly labeled as such, makes `max_tool_calls: 40`
  reachable; `Budget`/`Spend` structs match; per-tenant/global daily caps present.
  FR-F06-22 (dedupe) and FR-F06-23 (daily-cap downgrade) both present.
- **llm→rules fallback (DR-34):** §3.2 Resilience row states all six triggers, swap-only-forward,
  confidence cap; main-loop pseudocode and sequence diagram both encode it; AC-F06-24 covers it.
- **Prompt-injection requirements (DR-37):** FR-F06-4, FR-F06-13, §6 (7 numbered mechanisms),
  `Sanitizer` interface, canary/untrusted-wrap scheme, AC-F06-3 extended with the "no
  argument/proposal divergence vs. sanitized-copy run" clause.
- **Replay contract (DR-18):** `ReplayMode`/`Engine.Replay`, FR-F06-18/19/20/24, exact
  `ReplayRecorded`/`ReplayLiveDiff` semantics, `ErrEvidenceCorrupt` abort path, AC-F06-15/16/17.
- **Step persistence (DR-15/DR-18):** `model.Step` struct carries all 18 fields FR-F06-18
  requires; bodies in `store.ObjectStore`, never inline; AC-F06-18 checks this.
- **FR-F06-12 (predicate-after-conclusion) contradiction:** correctly split into FR-F06-12a
  (scope, at Contextualize) / FR-F06-12b (recurrence, only on Concluded+threshold), each
  annotated "(Was FR-F06-12; split per DR-11 §11.)" — no leftover single-phase FR-F06-12 text
  found (`grep -n "FR-F06-12[^ab]"` returns none).
- **Appendix C IDs:** FR-F06-12a/-12b/-18/-19/-20/-21/-22/-23/-24 all present; AC-F06-15 through
  AC-F06-24 all present — exact match to the Appendix C row for F06.
- **§2 drawback table:** D-Y1 (Dynatrace/replay) is concrete — cites `ReplayRecorded`/
  `ReplayLiveDiff` mechanics and specific ACs. D-X5 (predicate) is **not** in F06's table, but per
  `00-feature-catalog.md` D-X5 is canonically owned by **F02** (`D-J4, D-Z3, D-D2, D-T4, D-X1,
  D-X5`), not F06 (`D-J2, D-Z4, D-T3, D-D5, D-Y1, D-X2`); F02's own table already makes it
  concrete (`F02-tail-sampler.md` line 43, citing `SetInterestPredicate`/DR-11). This is correct
  per the catalog's ownership split, not a gap in F06.
- Mermaid (flowchart TB §4.1, sequenceDiagram §4.5) both syntactically well-formed.
- No leftover `rca.Report` local type, no leftover `StepStore` name (correctly noted as renamed).

No remaining gaps found in F06.

## F05-anomaly-detection.md — STATUS: spot-check COMPLETE, no defects found

Header lists exactly the DR set derived above.

Spot-checked:
- **FR-F05-11 deletion (severity/tier hard-page bypass, DR-21):** explicitly marked "Deleted
  (DR-21)" with rationale; `AC-F05-4` also marked deleted with a pointer to `AC-F05-16`;
  §1 states "there is no severity- or tier-derived bypass anywhere in this package"; §6 and §8
  reiterate `model.ServiceMeta.Tier` has no code path to `AlertRouter`, verified by AC-F05-16
  (AST/route test).
- **Appendix C IDs:** FR-F05-13/-14/-15 present (throughput_drop, Grouper.Add perf, e2e detection
  latency); AC-F05-12 through AC-F05-16 present — exact match to the Appendix C row for F05.
- **ONE paging rule (DR-21, three conditions P1/P2/P3):** §1 and the D-D4 drawback row state it;
  enforcement is explicitly located in `api.AlertRouter`, not F05, consistent with DR-21's intent.
- **Deploy-window abstraction (DR-14 §14.6):** `anomaly.DeployIndex` is the single owner
  (`Near`/`PrePostSplit`/`Record`/`ListWindow`), explicitly stated as shared with F06's
  `error-signature-new-after-deploy` rule — no duplicated pre/post split logic.
- `anomaly.REDSample` deletion (DR-39) confirmed — `model.REDSample` is the only RED type, local
  type explicitly marked deleted.
- `Grouper.ActiveIncidents` present (DR-13/DR-35 consumer), config-path-only citation discipline
  (DR-0) observed throughout §4.3.

One low-confidence observation, not treated as a defect: DR-31 asks that "every `F0x §4.3`
constructor signature" take a `Clock`. Neither F05 §4.3 nor F06 §4.3 shows concrete constructor
functions (`NewX(...)`) — both sections define interfaces only, with `Clock` usage referenced
in prose (F05 failure-modes row) rather than in a constructor signature. This may be consistent
with the doc template's level of abstraction (interfaces, not constructors) used throughout the
`F0x` series, so it is flagged as an ambiguity rather than a gap — raising rather than choosing,
per the register's own rule 5.

## Remaining / ambiguities

- DR-31 constructor-signature-with-`Clock` — see note above; would need a cross-doc check against
  another `F0x` doc's §4.3 to confirm the template convention before treating it as a gap.
- No other gaps identified in the time available. Given both files were already fully consistent
  with their claimed DR sets, no edits were made.
